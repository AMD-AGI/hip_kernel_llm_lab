#!/bin/bash
#
# HIP 编译优化 - 快速演示脚本
# 
# 使用方法:
#   bash quick_demo.sh
#
# 功能:
#   快速演示三大优化策略的效果
#

set -e  # 遇到错误立即退出

echo "════════════════════════════════════════════════════════════════════════════"
echo "🚀 HIP 编译优化 - 快速演示"
echo "════════════════════════════════════════════════════════════════════════════"
echo ""

# 检查源文件
HIP_FILE="hip_code_ex/hip_ref_hip_8825_EDMLoss.hip"
if [ ! -f "$HIP_FILE" ]; then
    echo "❌ 源文件不存在: $HIP_FILE"
    echo "请确保源文件路径正确"
    exit 1
fi

echo "📁 源文件: $HIP_FILE"
echo ""

# ============================================================================
# 演示 1: 增量编译
# ============================================================================

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "【演示 1】增量编译 - 智能跳过未修改文件"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

echo "🔨 首次编译..."
time python opt_incremental_compile.py --force

echo ""
echo "⚡ 再次编译（无修改，应该跳过）..."
time python opt_incremental_compile.py

echo ""
read -p "按 Enter 继续下一个演示..."
echo ""

# ============================================================================
# 演示 2: 并行编译
# ============================================================================

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "【演示 2】并行编译 - 充分利用多核CPU"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

NPROC=$(nproc)
echo "💻 CPU 核心数: $NPROC"
echo ""

echo "🔨 并行编译（使用 $NPROC 个进程）..."
time python opt_parallel_compile.py -j $NPROC

echo ""
read -p "按 Enter 继续下一个演示..."
echo ""

# ============================================================================
# 演示 3: 预编译头文件 (PCH)
# ============================================================================

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "【演示 3】预编译头文件 (PCH) - 加速头文件解析"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

echo "🔨 首次编译（生成 PCH）..."
time python opt_pch_compile.py --rebuild-pch

echo ""
echo "⚡ 再次编译（使用缓存的 PCH）..."
time python opt_pch_compile.py

echo ""
echo "📊 不使用 PCH 的对比..."
time python opt_pch_compile.py --no-pch

echo ""
read -p "按 Enter 查看总结..."
echo ""

# ============================================================================
# 总结
# ============================================================================

echo ""
echo "════════════════════════════════════════════════════════════════════════════"
echo "✅ 演示完成！"
echo "════════════════════════════════════════════════════════════════════════════"
echo ""
echo "💡 使用建议:"
echo ""
echo "  1️⃣  增量编译 - 适合频繁修改代码"
echo "     python opt_incremental_compile.py"
echo ""
echo "  2️⃣  并行编译 - 适合多文件项目"
echo "     python opt_parallel_compile.py -j 8"
echo ""
echo "  3️⃣  预编译头文件 - 适合复杂头文件项目"
echo "     python opt_pch_compile.py"
echo ""
echo "  🧪 运行完整性能测试:"
echo "     python run_all_benchmarks.py"
echo ""
echo "  📖 查看详细文档:"
echo "     cat QUICK_START.md"
echo ""
echo "════════════════════════════════════════════════════════════════════════════"

